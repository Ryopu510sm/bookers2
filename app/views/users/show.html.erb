<!--bookのindexページの左側の作成(User info)-->
<%= render '/books/books_index', user: @user, newbook: @newbook %>

<!--bookのshowページの右側の作成(Books)-->
    <div class="col-md-8 offset-md-1">

<!-- ユーザーの投稿一覧 -->
    <h1>Books</h1>
      <table class="table">
        <thead>
          <tr>
            <th></th>
            <th>Title</th>
            <th>Opinion</th>
          </tr>
        </thead>
        <tbody>
          <% @books.each do |book| %>
            <tr>
              <td>
              <% if book.user.profile_image.attached? %><!--画像の有無確認-->
                <%= image_tag book.user.profile_image, size: "100x100", alt: "ユーザーのプロフィール画像" %>
              <% else %>
                <%= image_tag 'no_image.jpg', size: "100x100", alt: "デフォルトのプロフィール画像" %>
              <% end %>
              </td>
              <td><%= link_to book.title, book_path(book.id) %></td>
              <td><%= book.body %></td>
            </tr>
          <% end %>
      
      </tbody>
      </table>
    </div>
  </div>
</div>


<!--Prefix Verb   URI Pattern                             Controller#Action-->
<!--users_show GET    /users/show(.:format)                  users#show-->